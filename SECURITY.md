# 安全政策

宣传记录助手处理来自互联网的网页，同时在本机生成和保存文件。安全边界是项目功能的一部分，而不是可选配置。

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| 1.0.x | 支持 |
| 早期开发版本 | 不支持 |

## 私下报告漏洞

请不要在公开 Issue 或 Discussion 中发布可直接利用的漏洞细节、用户数据或恶意样本。

发送邮件至 [2801572048@qq.com](mailto:2801572048@qq.com)，主题建议为“宣传记录助手安全报告”，并尽量包含：

- 受影响的版本与 Windows 版本；
- 漏洞类型、影响和复现步骤；
- 最小化测试网址或脱敏样本；
- 已尝试的缓解方式；
- 你希望采用的署名方式。

维护者会先确认收到报告，再评估修复与披露安排。请在修复发布前避免公开漏洞细节。

## 当前安全边界

- 本地服务仅监听 `127.0.0.1`，不对局域网开放。
- 页面 API 使用会话 Cookie、来源校验与扩展共享令牌。
- 只允许 `http`、`https` 网址，并拒绝带用户名/密码的 URL。
- 导航和子资源会校验目标地址，阻止回环、私网、链路本地与云元数据地址，跳转后再次验证。
- 每个任务使用隔离的临时 Edge 用户目录，不复用日常浏览器 Cookie。
- 扩展只把当前标签页截图发送到本机，不上传 Cookie、密码或文章内容。
- 任务历史和设置保存在本地 SQLite，PDF 写入用户选择的目录。

## 用户侧建议

- 只从本仓库 Releases 下载安装包，并核对附带的 SHA-256。
- 不要把来源不明的浏览器扩展目录加载到浏览器。
- 采集敏感材料前确认输出目录的 Windows 访问权限。
- 安装包尚无商业代码签名，SmartScreen 提示不能替代哈希校验。
